# Postgres credentials (used by docker-compose defaults)
POSTGRES_USER=ralts
POSTGRES_PASSWORD=ralts
POSTGRES_DB=ralts

# Optional: override DATABASE_URL to use sqlite instead of postgres:
# DATABASE_URL=sqlite+aiosqlite:///./ralts.db

# Optional: connect to an external postgres:
# DATABASE_URL=postgresql+asyncpg://user:password@hostname:5432/dbname
